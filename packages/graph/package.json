{
  "name": "@agenttrading/graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MarketGraph: nodes/edges, versioned snapshots, pathfinding, cycle detection, net costs. Depends only on contracts.",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agenttrading/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
