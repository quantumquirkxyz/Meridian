{
  "name": "@agenttrading/events",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "In-memory event bus with idempotency keys, SQLite (bun:sqlite) persistence of raw and normalized events, and deterministic replay. Depends only on contracts.",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agenttrading/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
