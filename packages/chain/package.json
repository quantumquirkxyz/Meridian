{
  "name": "@agenttrading/chain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "On-chain execution for DEX venues (PancakeSwap on BNB Chain) using viem. Depends only on contracts and viem.",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agenttrading/contracts": "workspace:*",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
