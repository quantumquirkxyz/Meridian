## Review Fix Implementation

Status: implemented

### Completed

- Fix commit message to follow Conventional Commits -> Amended from `BE-2: Update OpenAPI spec...` to `docs(openapi): update spec to reflect real backend contracts`

### Validation

- `git log -1 --format=%s` -> `docs(openapi): update spec to reflect real backend contracts`
- `git diff --check main...HEAD` -> PASS

### Remaining

None. Ready for next code-review pass.
